# Copy to .env and fill in the key for whichever provider your model uses.
# litellm reads these directly -- see https://docs.litellm.ai/docs/providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: where on-disk data lives. Defaults to ./workspace next to this file.
# WORKSPACE_DIR=./workspace
